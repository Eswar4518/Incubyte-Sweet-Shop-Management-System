# ============================================
# MongoDB Connection Configuration
# ============================================

# Local MongoDB connection
# Format: mongodb://host:port/database
# Change this if using MongoDB Atlas or remote server
MONGO_URI=mongodb://localhost:27017/sweet_shop

# Alternative for MongoDB Atlas (remote):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/sweet_shop?retryWrites=true&w=majority


# ============================================
# JWT (JSON Web Token) Configuration
# ============================================

# Secret key for signing JWT tokens
# IMPORTANT: Change this to a strong random string in production!
# Generate a strong secret: Use a tool like https://generate-secret.now.sh/
# Example: MONGO_URI=mongodb://localhost:27017/sweet_shop
JWT_SECRET=super-secret-key-change-in-production

# ============================================
# Server Configuration
# ============================================

# Port on which the API server listens
# Default: 4000
# Change if port 4000 is already in use
PORT=5000

RENDER_DEPLOY_HOOK=https://api.render.com/deploy/srv-d4va2pje5dus73aa9r60?key=dmG307GAx_Q

# Node environment
# Options: development, production, test
# Use 'development' for local development
# Use 'production' for deployed applications
# Use 'test' for running tests
NODE_ENV=development


# ============================================
# CORS Configuration
# ============================================

# Frontend URL allowed to make requests to this API
# This prevents CORS (Cross-Origin) errors
# Use http://localhost:3000 for React development server
# Change to your production frontend URL when deploying
CORS_ORIGIN=http://localhost:5173


# ============================================
# Database Configuration
# ============================================

# Database name (used by MongoDB)
# This will be created automatically if it doesn't exist
DB_NAME=sweet_shop


# ============================================
# ============================================
# SETUP INSTRUCTIONS
# ============================================
# ============================================

# 1. For Local Development (Recommended for learning):
#    - Keep MONGO_URI as: mongodb://localhost:27017/sweet_shop
#    - Install MongoDB locally: https://www.mongodb.com/try/download/community
#    - Start MongoDB: mongod (in a terminal)
#    - Run backend: npm run dev (in another terminal)

# 2. For Using MongoDB Atlas (Cloud Database):
#    - Create account at https://www.mongodb.com/cloud/atlas
#    - Create a cluster and get connection string
#    - Replace MONGO_URI with your Atlas connection string
#    - Example: mongodb+srv://user:pass@cluster.mongodb.net/sweet_shop?retryWrites=true&w=majority

# 3. For Production Deployment:
#    - Change NODE_ENV to 'production'
#    - Use a strong JWT_SECRET (generate random string)
#    - Update CORS_ORIGIN to your actual frontend URL
#    - Use MongoDB Atlas for cloud database
#    - Keep sensitive values in environment variables, NOT in code

# 4. Security Best Practices:
#    - NEVER commit .env file to Git (should be in .gitignore)
#    - NEVER share your JWT_SECRET
#    - NEVER use weak passwords in connection strings
#    - ALWAYS use HTTPS in production
#    - ALWAYS verify CORS_ORIGIN matches your frontend

# ============================================
# Quick Start Commands
# ============================================

# Install MongoDB locally (Windows):
# https://docs.mongodb.com/manual/tutorial/install-mongodb-on-windows/

# Start MongoDB (macOS/Linux):
# mongod

# Start MongoDB (Windows):
# "C:\Program Files\MongoDB\Server\5.0\bin\mongod.exe"

# Start backend:
# npm run dev

# Run tests:
# npm test

# Watch tests:
# npm run test:watch
